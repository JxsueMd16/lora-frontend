/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.lora.frontend',
  appName: 'LoRa Frontend',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;

